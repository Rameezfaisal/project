“Which Order BOMs belong to which 571-series NHA?”
Every downstream step (consumption, QOH, PO, demand rollups) uses this mapping.


newlist = (
    spark.table(tgt_odrbom_dmd)
    .select("odrbom", "nha")
    .dropDuplicates()
)