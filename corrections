CELL 15 — Final UNION (Materialized Result Set)
🧠 Business Meaning
We now combine all supplier discovery paths into a single result — just like the SAP procedure does with UNIONs.
In your dataset, only Cell 12 (All Suppliers – Part Line) produced rows, but we still union everything to keep logic faithful and future-proof.






final_supplier_inventory_df = (
    ob_supplier_df
    .unionByName(nha_supplier_df)
    .unionByName(ltb_supplier_df)
    .unionByName(rscxd_supplier_df)
    .unionByName(all_suppliers_part_df)
    .unionByName(all_suppliers_nha_df)
    .unionByName(all_suppliers_component_df)
).distinct()

final_supplier_inventory_df.cache()
final_supplier_inventory_df.count()