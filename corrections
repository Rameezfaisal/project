# Check which NHAs are duplicated and see the differences
duplicates_check = spark.sql(f"""
    SELECT * FROM {tgt_supplier_plants} 
    WHERE nha IN (
        SELECT nha FROM {tgt_supplier_plants} GROUP BY nha HAVING count(*) > 1
    )
    ORDER BY nha
""")
display(duplicates_check)
