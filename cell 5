print("--- 1. MKPF (bldat) ---")
mkpf.select("bldat").printSchema()
mkpf.select("bldat").show(5, truncate=False)

print("\n--- 2. SPARES (actual_gi) ---")
spares.select("actual_gi").printSchema()
spares.select("actual_gi").show(5, truncate=False)

print("\n--- 3. PO (due_dt) ---")
po.select("due_dt").printSchema()
po.select("due_dt").show(5, truncate=False)
