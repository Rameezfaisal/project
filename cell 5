    .withColumn(
    "dmddt",
    F.when(
        (F.trim(F.col("dmddt")).isin("", "00000000", "0")) | F.col("dmddt").isNull(),
        F.to_date(F.lit("19000101"), "yyyyMMdd")
    ).otherwise(
        F.to_date(F.col("dmddt"), "yyyyMMdd")
    ))
