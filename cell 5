# 10.3 -- Combine PO and PR suppliers
# FIX: Add .dropDuplicates(["nha"]) to ensure 1:1 match for Delta Merge

df_po_pr = (
    df_po_split
    .join(df_pr_supp, "nha", "left")
    .dropDuplicates(["nha"])  # <--- THIS IS THE FIX
)
